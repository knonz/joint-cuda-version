cmake_minimum_required(VERSION 2.8)
project( DisplayImage )
#set(CMAKE_PREFIX_PATH "`pkg-config opencv --cflags --libs ~/.local/lib/pkgconfig/opencv.pc`")
find_package(OpenMP)
if (OPENMP_FOUND)
    set (CMAKE_C_FLAGS "${CMAKE_PREFIX_PATH} ${CMAKE_C_FLAGS} ${OpenMP_C_FLAGS}")
    set (CMAKE_CXX_FLAGS "${CMAKE_PREFIX_PATH} ${CMAKE_CXX_FLAGS} ${OpenMP_CXX_FLAGS}")
    set (CMAKE_EXE_LINKER_FLAGS "${CMAKE_EXE_LINKER_FLAGS} ${OpenMP_EXE_LINKER_FLAGS}")
endif()
find_package( OpenCV REQUIRED )
    set( OpenCV_DIR "pkg-config opencv --cflags --libs ~/.local/lib/pkgconfig/opencv.pc" )
#    set( PKG_CONFIG_PATH "/usr/lib/pkgconfig" )
add_executable( DisplayImage DisplayImage_CU.cpp )
#add_executable( DisplayImage test.cpp )
target_link_libraries( DisplayImage ${OpenCV_LIBS} )
